/**
 * The Hostinger referral (in code, not in /admin-cp/referrals; ADR 0021). Its QR code points to
 * /go/hostinger like the other partners, so a printed code keeps working if the link changes.
 */
export const HOSTINGER_REFERRAL_URL = 'https://www.hostinger.com?REFERRALCODE=BYLLORIKRXAQ';
export const HOSTINGER_SLUG = 'hostinger';
