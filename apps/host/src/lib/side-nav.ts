// Plain constants shared by the server (initial sidebar width) and the sidebar itself.

/** Cookie remembering whether the sidebar is collapsed to icons (this site only, 1 year). */
export const SIDENAV_COOKIE = 'dq_sidenav';
export const SIDENAV_COOKIE_MAX_AGE = 60 * 60 * 24 * 365;
